<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Goal</title>
</head>
<body>
    <h1>Edit Goal</h1>

    <% if (goal) { %>
        <form action="/goal/update/<%= goal.id %>" method="POST">
            <label for="title">Title:</label><br>
            <input type="text" id="title" name="title" value="<%= goal.title %>" required><br><br>

            <label for="description">Description:</label><br>
            <textarea id="description" name="description"><%= goal.description %></textarea><br><br>

            <button type="submit">Update Goal</button>
        </form>
    <% } else { %>
        <p>Goal not found.</p>
    <% } %>

    <br>
    <a href="/goal">Back to Goals List</a>
</body>
</html>
