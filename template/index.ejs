<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <h1>Goals</h1>
    <ul>
        <% goals.forEach(goal => { %> 
            <li>
                <strong><%= goal.title %></strong>: <%= goal.description %> 
                <br>Created at: <%= goal.created_at %>
                <form action="/goal/delete/<%= goal.id %>?_method=DELETE" method="POST" style="display:inline;">
                    <button type="submit" onclick="return confirm('Are you sure you want to delete this goal?')">Delete</button>
                </form>
            </li>
        <% }) %>
    </ul>
</body>
</html>